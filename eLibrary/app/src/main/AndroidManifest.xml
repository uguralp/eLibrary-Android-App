<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uguralp.elibrary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ItemsActivity" />
        <activity android:name=".ViewBookActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".AddItemActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".AddtoCardActivity" />
        <activity android:name=".PayItemActivity" />
        <activity android:name=".PaymentCompletedActivity" />
        <activity android:name=".ViewSignedOut"></activity>
    </application>

</manifest>